spring:
  application:
    name: discovery-service
  profiles:
    active: default
  cloud:
    config:
      uri: http://config-service:8088
      
management:
  endpoints:
    web:
      exposure:
        include: '*'